# 功能补齐与验收记录

## 本轮交付

- 短链按绑定域名返回完整 PublicURL，保留端口；普通账号读取链接无需管理员域名权限。不可用域名不回退成相对地址。
- 群活码表单传递落地页和落地域名；独立目标管理支持素材、启停、阈值、权重、优先级、到期和群主。更新不覆盖计数，扫码采用数据库原子条件更新，达到阈值自动切换。
- 微信分享卡片管理、封面素材、公众号加密配置、票据缓存、签名和公开分享页面。停用卡片返回 404。
- 入口二维码在浏览器生成，可预览和下载 PNG。
- Docker 重建应用镜像，原业务数据卷保留；新增表和目标字段执行增量迁移。

## 证据与范围

- Go 全量测试、构建通过；前端类型检查、现有 13 项测试、生产构建通过。
- `scripts/e2e_content.py`：16 项新增检查，包含真实 URL（不注入 Host）、目标阈值/到期/恢复、20 次并发只分配 3 次限额目标、上传校验、卡片页面/停用和权限。
- `scripts/e2e_initialization.py`：临时应用实例、独立数据库，4 项初始化检查 + 上述 16 项功能检查通过；主项目不被该脚本重置。
- 既有 `e2e_local.py` 的 66 项检查通过。
- 浏览器成功完成卡片创建、素材选择、公开预览、群码创建和目标读取；打开 `http://localhost:18080/x` 实际到达 `https://example.com/verified?from=gravitylink`。
- 微信签名标准测试向量、密钥加解密/篡改拒绝、卡片 HTML 转义和域名端口测试通过。未配置真实公众号，不能据此宣称已通过微信客户端分享验收。

## 本轮测试事故

旧版 `e2e_lifecycle.py` 包含重置/重新初始化步骤，本轮运行时执行了该步骤。业务记录与统计保留，但系统设置被清空，和此前承诺不重置主项目不一致。已改为默认不执行重置，需显式 `--allow-reset` 才进入该分支；后续完整初始化测试使用独立实例。没有可用的清空前设置快照，不能宣称已恢复所有设置。

## 本机使用

管理端 `http://127.0.0.1:18081`；公开入口配置为 `http://localhost:18080`，落地服务配置为 `http://127.0.0.1:18080`。本机测试数据中的 `.test` 域名没有公共 DNS；不要选作对外分享域名。手机使用需配置可到达此服务的域名及 HTTPS。

分享卡片 → 公众号配置中填写自有 AppID/AppSecret，并完成公众号侧 IP 白名单和 JS 接口安全域名配置。截图中的凭据未使用。微信官方文档入口：https://developers.weixin.qq.com/doc/service/guide/h5/jssdk.html 。本轮网络工具未成功读取官方页面，实现参考旧版调用及协议测试向量，接入后仍需真机验证。

数据目录中的 `wechat.key`、上传目录和数据库需共同备份。新增的独立验收数据库保留用于核查；临时应用容器已退出。
